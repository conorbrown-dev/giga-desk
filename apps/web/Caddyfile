:{$PORT:8080} {
  handle /healthz {
    respond "healthy" 200
  }

  handle /api/* {
    reverse_proxy {$API_UPSTREAM}
  }

  handle_path /auth/* {
    reverse_proxy {$KEYCLOAK_UPSTREAM}
  }

  handle {
    root * /srv
    try_files {path} /index.html
    file_server
  }
}
